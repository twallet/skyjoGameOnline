import {
  consoleLogger as frontendConsoleLogger,
  resolveLogger as frontendResolveLogger,
} from "../../frontend/utils/logger.js";

export const consoleLogger = frontendConsoleLogger;
export const resolveLogger = frontendResolveLogger;

